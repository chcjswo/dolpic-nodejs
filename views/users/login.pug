extends ../layout/layout-web

block content
	include ../partials/header-web
	<!-- login -->
	if errors
		ul
			for error in errors
				li= error.msg
	.login_box
		p 아이돌, 유명 연예인 SNS 이미지를 한 번에 모아보세요.
		form(action="/users/login" method="POST" name="form")
			input(type="text" name="username" placeholder="아이디")
			input(type="password" name="password" placeholder="password")
			input(type="hidden" name="returnUrl" value=returnUrl)
		.clear
		.btn_login 로그인
		.signup_text
			a(href='/users/signup' target='_self') 돌픽 처음이신가요? 회원가입 &gt;&gt;